requireRole('admin'); async function load(){ const users=await api('/users'); document.querySelector('#users').innerHTML=users.map(u=>'<tr><td>'+u.id+'</td><td>'+u.name+'</td><td>'+u.email+'</td><td><select onchange="role('+u.id+',this.value)" class="input"><option '+(u.role==='admin'?'selected':'')+'>admin</option><option '+(u.role==='researcher'?'selected':'')+'>researcher</option><option '+(u.role==='user'?'selected':'')+'>user</option></select></td><td><button class="btn danger" onclick="del('+u.id+')">Delete</button></td></tr>').join(''); } async function role(id,r){ await api('/users/'+id+'/role',{method:'PUT',body:JSON.stringify({role:r})}); msg('Role updated',true); } async function del(id){ if(confirm('Delete user?')){ await api('/users/'+id,{method:'DELETE'}); load(); } } document.addEventListener('DOMContentLoaded', load);
